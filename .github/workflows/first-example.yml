name: My first workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: welcome message
          run: echo "My first Github Action"
        
        - name: list file
          run: ls

        - name: Read files
          run: cat README.md